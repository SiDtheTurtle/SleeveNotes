# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Database & runtime data
*.db
data/
# blank.db has no personal data and is committed (negation must follow *.db)
!tests/fixtures/blank.db

# Test secrets and fixtures (personal data — backed up on NAS)
# Backup zips embed a live discogs_token and api_key — never commit them.
tests/.env.test
tests/fixtures/*.zip
tests/fixtures/golden.db
tests/fixtures/edge/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local config (contains personal paths/permissions)
.claude/settings.local.json

# Working specs (local only — not shipped)
docs/fr-*.md
